<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>HEY</h1>
    <h2>welcome to the site</h2>
    <h3>the messaging site</h3>
    <h4>make an account, login, post a message</h4>
    <hr>
        <a href="/createUser">Create an account</a> | 
        <a href="/login">Login</a> | 
        <a href="/message">post a message</a> | 
    <hr>

    <h2>Messages</h2>

    <% for (let message of locals.messages) { %> 
        <div>
            <%= message.dataValues.content %> 
        </div>
        <div>
            <button class="likes" id="<%-message.dataValues.id%>" onclick="countlikes(this)">like</button>
        </div>
    <% } %>
    
<Script>
    let count = 0
    function countlikes(btn){
        //alert(btn.id)
        count++
        //not perfect yet XD
        //alert('clicked' + count)
        btn.innerHTML = count + ' likes'
    }
</Script>
</body>
</html>